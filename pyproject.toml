[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.build_meta"

[project]
name = "gbrain-communication"
version = "0.1.0"
description = "Connect and collaborate before you even meet"
requires-python = ">=3.10"
dependencies = []

[project.scripts]
gbrain = "gbrain.cli:main"

[tool.pytest.ini_options]
testpaths = ["tests"]
